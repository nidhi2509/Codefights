def adjacentElementsProduct(inputArray):
    maxp = inputArray[0]*inputArray[1]
    for i in range(len(inputArray)-1):
            if inputArray[i]*inputArray[i+1] > maxp:
                maxp = inputArray[i]*inputArray[i+1]
    return maxp
